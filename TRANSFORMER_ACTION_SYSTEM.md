# 1. 学习吃透与实验 Transformer：执行系统

这个文档不是读书计划，而是行动系统。目标是把“我想真正理解 Transformer”变成每次打开项目都能完成、能记录、能反馈的小动作。

核心原则：

- 主线只保留一个：吃透并实验 Transformer。
- 每次只推进一个最小任务，完成比完美重要。
- 学习必须落到项目、代码、实验或笔记之一。
- 遇到新兴趣先记录到“探索停车场”，不立即切换主线。
- 每个阶段用结果复盘，不用情绪复盘。

## 1.1. 目标

最终你应该能做到：

- 能用自己的话解释 Token、Embedding、Positional Encoding、Q/K/V、Attention、Encoder、Decoder、Loss、Backprop、Checkpoint、Inference。
- 能从训练数据 `A/B` 解释训练时为什么使用真实 B 前缀，推理时为什么使用模型生成前缀。
- 能读懂本项目的核心训练与推理流程。
- 能修改模型配置并观察效果，例如 `num_layers`、`embed_dim`、`num_heads`、`batch_size`、`max_len`。
- 能设计一个小实验，跑完、记录结果、得出结论。
- 能向别人讲清楚：“Transformer 是如何通过 next-token prediction 学到东西的。”

## 1.2. 单次执行最小闭环

每次打开项目只要求完成下面 4 步。时间不够时，只做 10-25 分钟版本。

```text
1. 选一个任务
2. 做 25-60 分钟
3. 留下一个可见产物
4. 写 3 行记录
```

可见产物可以是：

- 一段运行成功的代码
- 一条实验结果
- 一个你亲手画出的流程
- 一段解释笔记
- 一个修改过的函数
- 一个新的问题清单

单次记录模板：

```markdown
### YYYY-MM-DD

- 本次任务：
- 产物：
- 我这次真正弄懂了：
- 卡住的问题：
- 下一步：
```

## 1.3. 阶段主线

不限定每个阶段必须在几天或几周内完成。从第 1 阶段开始逐个阶段推进，达到当前阶段的完成标准后，再进入下一阶段。

### 1.3.1. 第 1 阶段：建立整体地图

目标：先知道整个系统每一块在干什么，不追求细节全懂。

- [ ] 画出完整流程：`数据 -> Tokenizer -> Batch -> Embedding -> Transformer -> Generator -> Loss -> Backward -> Checkpoint`
- [ ] 用自己的话解释训练数据中的 `A` 和 `B`
- [ ] 用自己的话解释：训练时为什么使用真实 B 前缀
- [ ] 用自己的话解释：推理时为什么使用模型生成 B 前缀
- [ ] 解释 `<BOS>`、`<EOS>`、`<PAD>` 分别解决什么问题
- [ ] 找到项目中训练入口、推理入口、checkpoint 保存入口
- [ ] 写一页笔记：这个项目到底训练了一个什么模型

完成标准：

- 能不看资料，把训练和推理流程讲一遍。
- 能解释为什么 `checkpoint_1.pt` 是权重状态，而不是完整大模型产品。

### 1.3.2. 第 2 阶段：吃透 Tensor 和形状

目标：不再被 `shape` 绊住。

- [ ] 复习 tensor 的基本概念：维度、shape、batch、sequence、hidden size
- [ ] 手写一个例子：`[batch, seq_len] -> [batch, seq_len, embed_dim]`
- [ ] 理解 `reshape/view/transpose/permute` 的区别
- [ ] 记录 Attention 中常见 shape：
  - `X: [batch, seq_len, embed_dim]`
  - `Q/K/V: [batch, heads, seq_len, head_dim]`
  - `scores: [batch, heads, seq_len, seq_len]`
  - `attention output: [batch, seq_len, embed_dim]`
- [ ] 做一个小实验：打印一次前向传播中关键 tensor 的 shape
- [ ] 写一页笔记：为什么多头注意力要拆成多个 head

完成标准：

- 看到 `[batch, heads, seq_len, head_dim]` 不再慌。
- 能解释 `seq_len x seq_len` 的 attention matrix 表示什么。

### 1.3.3. 第 3 阶段：吃透 Attention 和 Q/K/V

目标：真正理解 Q/K/V 是怎么参与学习的。

- [ ] 用一句话解释 `W_Q/W_K/W_V` 和 `Q/K/V` 的区别
- [ ] 手写一个极小例子：2 个 Token、2 维向量，计算一次 `QK^T`
- [ ] 解释为什么要除以 `sqrt(d_k)`
- [ ] 解释 softmax 如何把 attention scores 变成权重
- [ ] 解释 `attention @ V` 为什么是“加权取信息”
- [ ] 观察一次训练前后的 attention 分布是否变化
- [ ] 写一页笔记：Attention 不是“模型在思考”，而是可学习的信息路由机制

完成标准：

- 能讲清楚：训练不会直接保存 Q/K/V，而是保存 `W_Q/W_K/W_V`。
- 能讲清楚：Loss 如何通过反向传播影响 QKV 权重。

### 1.3.4. 第 4 阶段：吃透 Loss 和反向传播

目标：理解模型为什么能“学到东西”。

- [ ] 用你的 `A/B` 例子写出 next-token 训练表
- [ ] 手算一次 Cross Entropy：`Loss = -log(P(correct_token))`
- [ ] 对比三种情况：
  - 正确 Token 概率 0.8
  - 正确 Token 概率 0.1
  - 正确 Token 概率 0.001
- [ ] 解释为什么训练时通常不需要 `argmax`
- [ ] 解释错误 Token 的离散 ID 为什么不直接参与 Loss
- [ ] 解释错误 Token 的 logit/probability 为什么会间接影响 Loss
- [ ] 写一页笔记：Cross Entropy 到底在奖励什么、惩罚什么
- [ ] 写一页笔记：`loss.backward()` 不是魔法，它是在计算每个参数如何影响 loss

完成标准：

- 能准确解释：“训练时取正确 Token 的概率计算 Loss，但整个词表的 logits 都影响梯度。”

### 1.3.5. 第 5 阶段：吃透 Encoder-Decoder 翻译

目标：理解本项目和 GPT 类模型的区别。

- [ ] 解释 Encoder 的输入是什么
- [ ] 解释 Decoder 的输入是什么
- [ ] 解释 Decoder self-attention 和 cross-attention 的区别
- [ ] 用 `A1 = ab c def`、`B1 = 我 怕 鬼 啊 洛 桑` 写出训练过程
- [ ] 用同一组 `A1/B1` 写出推理过程
- [ ] 解释为什么训练时可以并行预测所有位置
- [ ] 解释为什么推理时只能一步一步生成
- [ ] 写一页笔记：Encoder-Decoder 和 Decoder-only 的区别

完成标准：

- 能清楚说出：GPT 类模型通常把 prompt、对话历史、已生成内容拼成一个上下文序列；而翻译模型通常有明确的 source 和 target。

### 1.3.6. 第 6 阶段：实验模型配置

目标：从“看懂”进入“动手验证”。

每次只改一个变量，记录结果。

- [ ] 改小 `num_layers`，观察参数量、训练速度、效果
- [ ] 改小 `embed_dim`，观察参数量、训练速度、效果
- [ ] 改变 `num_heads`，确认 `embed_dim` 必须能被 `num_heads` 整除
- [ ] 改变 `batch_size`，观察显存/内存和训练速度
- [ ] 改变 `max_len` 或生成长度，观察输出
- [ ] 对比保存 checkpoint 前后的文件大小
- [ ] 对比是否 tie weights 对参数量的影响
- [ ] 记录一次完整实验：假设、修改、结果、结论

实验记录模板：

```markdown
### 实验：标题

- 日期：
- 假设：
- 修改：
- 运行命令/方式：
- 观察结果：
- 结论：
- 下一个实验：
```

完成标准：

- 至少完成 5 个小实验。
- 每个实验都有结论，而不是只跑了一下。

### 1.3.7. 第 7 阶段：理解推理与生成策略

目标：知道模型如何从概率分布变成文本。

- [ ] 解释 greedy decoding
- [ ] 解释为什么 greedy 不一定得到全局最优句子
- [ ] 实现或伪代码写出 top-k sampling
- [ ] 实现或伪代码写出 temperature sampling
- [ ] 比较同一个输入下 greedy 和 sampling 的输出差异
- [ ] 观察模型什么时候生成 `<EOS>`
- [ ] 增加“遇到 `<EOS>` 提前停止”的逻辑设计
- [ ] 写一页笔记：概率分布、采样、生成多样性的关系

完成标准：

- 能解释模型为什么每一步都输出整个词表概率，而不是直接输出一句话。

### 1.3.8. 第 8 阶段：整理成可展示成果

目标：把学习变成可以分享的成果。

- [ ] 写一篇笔记：Transformer 如何通过 Loss 和反向传播学到 QKV 权重
- [ ] 写一篇笔记：训练时真实历史 Token vs 推理时模型生成 Token
- [ ] 整理一张图：训练流程
- [ ] 整理一张图：推理流程
- [ ] 整理一张图：Attention 中 Q/K/V 的计算
- [ ] 写一个最小版 Transformer 伪代码
- [ ] 写一个 README 补充：如何运行、如何训练、如何加载 checkpoint
- [ ] 做一次 10 分钟口头讲解，并记录卡住的点

完成标准：

- 能把这个项目讲给另一个人听。
- 对方问“模型怎么学会的”，你能不用术语堆砌解释清楚。

## 1.4. 阶段节奏

不按星期推进，不设置阶段截止日期。只遵守一个顺序：

```text
第 1 阶段完成
    ↓
第 2 阶段完成
    ↓
第 3 阶段完成
    ↓
继续下一个阶段
```

### 1.4.1. 阶段推进规则

- 当前只推进一个阶段。
- 每次打开项目时，先看当前阶段的 checkbox。
- 每次只选一个最小任务，不跨阶段跳任务。
- 当前阶段的完成标准没有达到，不进入下一阶段。
- 如果卡住，可以临时做一个小实验或补一页笔记，但仍然服务于当前阶段。
- 新兴趣放入“探索停车场”，不抢当前阶段的主线。

### 1.4.2. 单次执行方式

每次执行时按这个顺序：

```text
1. 打开当前阶段
2. 选一个未完成 checkbox
3. 做 10-60 分钟
4. 留下一个可见产物
5. 写一条完成记录
```

### 1.4.3. 阶段复盘

每完成一个阶段后，再回答这 6 个问题：

```markdown
### 第 X 阶段复盘

- 本阶段完成了什么：
- 哪个概念更清楚了：
- 哪个实验或笔记带来了真实反馈：
- 哪个地方还不稳：
- 是否达到本阶段完成标准：
- 下一阶段开始任务：
```

只有当“是否达到本阶段完成标准”能明确回答“是”时，才进入下一阶段。

## 1.5. 探索停车场

看到新兴趣先放这里，不立即展开。

- [ ] GPT 类 Decoder-only 模型
- [ ] BERT 类 Masked Language Model
- [ ] RLHF / DPO / 偏好优化
- [ ] tokenizer 原理
- [ ] MPS / CUDA / GPU 加速
- [ ] 模型量化
- [ ] LoRA / fine-tuning
- [ ] 多模态模型
- [ ] Agent 和工具调用
- [ ] 训练数据清洗

## 1.6. 防跑偏规则

当你想切换方向时，先回答：

```text
这个新方向是否直接帮助我吃透 Transformer？
如果不是，先放入探索停车场。
```

当你觉得“我还不够懂”时，先做一个小动作：

```text
打开项目 -> 选一个函数 -> 写出输入 shape 和输出 shape
```

当你觉得“这次时间太少”时，只做 10 分钟：

```text
写 3 行笔记，或者勾掉一个最小 checkbox
```

当你觉得“学这个有什么意义”时，回到主目标：

```text
我不是为了马上成为专家。
我是为了把宏大的好奇心，训练成稳定的现实反馈。
```

## 1.7. 完成记录

把完成过的动作写在这里，让大脑看到真实进展。


### 1.7.1. 阶段一
#### 1.7.1.1. 2026-09-14

- 本次任务：画出完整流程
- 产物：流程图
- 我这次真正弄懂了：Transform架构原理
- 卡住的问题：
- 下一步：

```markdown
input -> embinding -> tokenizer -> postion encode -> Encode Attion Layer (Mulithead) -> Decode Attion Layer -> ouput
```